import {Module} from "simple-boot-core/module/Module";
import {IncomingMessage, ServerResponse} from "http";

export class ReceiveModule extends Module {

    receive(req: IncomingMessage, res: ServerResponse) {

    }
}
